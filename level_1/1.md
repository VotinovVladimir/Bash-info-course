## Введение в Bash

### 1️⃣ Что такое оболочка (Shell)

**Оболочка** — это программа, которая предоставляет интерфейс между пользователем и операционной системой.
Она выполняет три основные задачи:

1. **Интерпретация команд**
   * Пользователь вводит команды в терминале.
   * Оболочка читает эти команды, интерпретирует их и передаёт ядру ОС для выполнения.
2. **Запуск программ**
   * Любая программа (например, `ls`, `cat`, `python`) запускается через оболочку.
3. **Автоматизация**
   * Оболочка умеет запускать сценарии (скрипты), автоматизируя рутинные задачи.

Пример: если ты пишешь команду в терминале:

```
ls -l /home/user
```

* Оболочка читает команду `ls` и аргументы `-l /home/user`.
* Передаёт системе для выполнения.
* Отображает результат на экране.

### 2️⃣ Зачем нужна оболочка

* **Управление системой**: запуск программ, управление файлами, настройка сети.
* **Автоматизация**: можно писать скрипты, которые выполняют задачи без твоего участия.
* **Работа с потоками данных**: перенаправление ввода/вывода, конвейеры (`|`).
* **Интерактивная работа**: ввод команд, навигация по файлам, проверка состояния системы.

Без оболочки пользователь не мог бы напрямую взаимодействовать с ядром ОС — пришлось бы писать команды на низкоуровневом языке.

### 3️⃣ Типы оболочек

Существует несколько популярных оболочек в UNIX/Linux. Основные:


| Оболочка             | Особенности                                                                                                                                                            | История/Применение                                                                                              |
| ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| **Bash**(Bourne Again Shell) | Совместима с Bourne Shell (`sh`), добавляет расширенные функции, скрипты, историю команд, автодополнение. | Самая популярная оболочка в Linux и macOS, часто используется для скриптов. |
| **sh**(Bourne Shell)         | Классическая оболочка, простая, стандарт POSIX.                                                                                                | Используется в скриптах, которые должны работать на любой UNIX-системе.  |
| **csh**(C Shell)             | Синтаксис похож на C, поддержка истории команд, алиасов.                                                                             | Редко используется, исторически популярен среди программистов на C.     |
| **tcsh**                     | Расширение csh: автодополнение, улучшенная история команд.                                                                         | Применяется для удобства интерактивной работы.                                          |
| **ksh**(Korn Shell)          | Совмещает возможности sh и csh, поддержка функций и массивов.                                                                       | Используется в корпоративных UNIX-системах.                                                    |
| **zsh**                      | Очень гибкая оболочка, мощное автодополнение, плагины, тема оформления.                                               | Современный выбор для интерактивной работы, часто заменяет Bash.              |

💡 **Bash** — это стандартная оболочка для большинства Linux-дистрибутивов и удобный выбор для начинающих.
