## 📘 Работа с каталогами

Каталоги (папки) — это то, что придаёт порядок любому набору файлов и данных. В работе DevOps-инженера или системного администратора они помогают организовывать конфигурации, логи, проекты и временные файлы, создавая понятную структуру файловой системы. Умение создавать, удалять и управлять каталогами необходимо для автоматизации задач, работы с серверами и Docker-контейнерами.

В Linux, macOS и WSL файловая система строится иерархически, начиная с корневой папки `/`. Каждая папка может содержать файлы и подкаталоги, формируя дерево директорий, которое удобно использовать для организации проектов и автоматизации процессов.

### Команда `mkdir`

Команда `mkdir` (от **make directory** — «создать каталог») создаёт новые папки в файловой системе.

Примеры использования:

<pre class="overflow-visible!" data-start="747" data-end="949"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre! language-bash"><span><span>mkdir</span><span> Projects              </span><span># создаёт папку Projects в текущем каталоге</span><span>
</span><span>mkdir</span><span> Test                  </span><span># создаёт папку Test</span><span>
</span><span>mkdir</span><span> -p dir1/dir2/dir3     </span><span># создаёт сразу несколько вложенных папок</span><span>
</span></span></code></div></div></pre>

Ключ `-p` позволяет создавать сразу несколько уровней директорий, если они ещё не существуют, что удобно для подготовки структуры проекта.

### Команда `rmdir`

Команда `rmdir` удаляет пустые каталоги. Аббревиатура `rmdir` — **remove directory** — «удалить каталог».

Примеры использования:

<pre class="overflow-visible!" data-start="1250" data-end="1388"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre! language-bash"><span><span>rmdir</span><span> Test                  </span><span># удаляет пустую папку Test</span><span>
</span><span>rmdir</span><span> Projects/Old          </span><span># удаляет пустую папку Old внутри Projects</span><span>
</span></span></code></div></div></pre>

Важно: `rmdir` удаляет только пустые каталоги. Если папка содержит файлы или другие папки, она не будет удалена.

### Команда `rm -r`

Для удаления каталогов с содержимым используется команда `rm` с ключом `-r` (от **recursive** — «рекурсивно»). Она позволяет удалять папки вместе со всеми файлами и подкаталогами.

Примеры использования:

<pre class="overflow-visible!" data-start="1737" data-end="1911"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre! language-bash"><span><span>rm</span><span> -r Projects/Test         </span><span># удаляет папку Test и всё её содержимое</span><span>
</span><span>rm</span><span> -rf OldProject           </span><span># принудительно и рекурсивно удаляет OldProject без подтверждений</span><span>
</span></span></code></div></div></pre>

Ключ `-f` (force) делает удаление принудительным, без запросов на подтверждение. Будьте осторожны: удалённые данные не восстанавливаются через терминал.

### Полезные заметки

* Всегда проверяйте содержимое каталога перед удалением с помощью `ls` или `tree`.
* Используйте автодополнение `Tab`, чтобы не ошибиться в имени папки.
* Для резервного копирования перед удалением полезно скопировать каталог с помощью `cp -r`.
* Комбинируя команды, можно создавать сложные структуры проектов и безопасно их удалять.

### Практика

1. Создайте папку `Projects` с помощью `mkdir`.
2. Внутри `Projects` создайте папки `Frontend`, `Backend` и `Docs`.
3. Создайте вложенную структуру папок: `Projects/Backend/API`.
4. Используя `tree`, проверьте структуру каталогов.
5. Удалите пустую папку `Docs` с помощью `rmdir`.
6. Попробуйте удалить папку `Backend` с помощью `rmdir` и объясните результат.
7. Используйте `rm -r` для удаления папки `Backend` вместе со всеми вложенными каталогами.
8. Создайте папку `Temp` с несколькими файлами внутри, а затем удалите её рекурсивно.
9. Попробуйте использовать `rm -rf` для принудительного удаления, убедившись, что нет важных данных.
10. После удаления всех тестовых папок, проверьте текущее состояние с помощью `ls` или `tree`.
