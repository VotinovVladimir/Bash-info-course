## 📘 Терминал Linux и Windows (WSL, PowerShell)

## Что такое терминал и почему он важен для DevOps

**Терминал** — это инструмент, через который DevOps-инженер управляет инфраструктурой, серверами, контейнерами, пайплайнами и облаками.
Он даёт прямой доступ к ядру системы и позволяет:

* выполнять команды, скрипты и автоматизировать рутину;
* подключаться по SSH к серверам и кластеру;
* запускать CI/CD пайплайны, деплой и тестирование;
* анализировать логи и состояние системы.

### Применение в DevOps:


| Ситуация               | Что делает терминал                                                                             |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------- |
| Деплой на сервер | `scp`/`rsync`для копирования файлов,`ssh`для входа, запуск bash-скрипта |
| Настройка Docker      | Команды`docker run`,`docker exec`,`docker logs`выполняются в терминале               |
| CI/CD                          | Bash-скрипты выполняются в GitHub Actions / Jenkins / GitLab CI                               |
| Мониторинг           | Проверка состояния сервисов (`systemctl status`,`ps aux`,`journalctl`)                  |

## Оболочки: bash, sh, zsh, PowerShell

**Оболочка (shell)** — программа, которая принимает команды, обрабатывает их и взаимодействует с ядром.
Для DevOps особенно важен **bash**, потому что:

* он стандарт POSIX — работает одинаково на Linux, macOS и WSL;
* почти все CI/CD инструменты используют bash-сценарии;
* Docker и Kubernetes CLI-команды работают через bash;
* большинство системных автоматизаций пишутся именно на bash.

### Применение в DevOps:


| Оболочка | Применение                                                                                                           |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| **bash**         | CI/CD, Docker entrypoints, systemd, startup-скрипты                                                                     |
| **sh**           | Минимальные скрипты (например, внутри Docker-образов`alpine`)                           |
| **zsh**          | Удобная интерактивная оболочка для разработчиков (локальная работа) |
| **PowerShell**   | DevOps в Windows-инфраструктурах (Azure, AD, IIS и т.д.)                                                    |

## Linux, WSL и PowerShell — где что применяется


| Среда                            | Где используется                    | Типичные DevOps-задачи                                     |
| ------------------------------------- | -------------------------------------------------- | ------------------------------------------------------------------------ |
| **Linux**                             | Серверы, контейнеры, CI/CD runner | Настройка сервисов, деплой, мониторинг  |
| **WSL (Windows Subsystem for Linux)** | Разработка под Windows                | Работа с bash, docker, git, тестирование              |
| **PowerShell**                        | Windows-сервера                             | Настройка Windows Server, IIS, Azure автоматизация |

### Пример:

> Разработка скрипта деплоя Docker-контейнера происходит в WSL (bash), затем он заливается на Linux-сервер и выполняется через SSH.
> А если инфраструктура в Windows — скрипты выполняются PowerShell.

## Файловая структура Linux — фундамент для DevOps

DevOps постоянно работает с системными путями, логами, конфигами.
Поэтому важно знать:


| Каталог | Назначение                                               |
| -------------- | ------------------------------------------------------------------ |
| `/etc`         | Конфигурации сервисов (nginx, systemd, docker) |
| `/var/log`     | Логи сервисов                                          |
| `/usr/bin`     | Исполняемые программы                          |
| `/home`        | Домашние директории пользователей   |
| `/tmp`         | Временные файлы                                      |

### Применение в DevOps:

* Анализ логов nginx: `cat /var/log/nginx/error.log`
* Проверка конфигов Docker: `/etc/docker/daemon.json`
* Настройка systemd сервисов: `/etc/systemd/system/`

## Основные команды (базовые DevOps-команды)


| Команда        | Назначение                    | Пример DevOps-задачи                                         |
| --------------------- | --------------------------------------- | ------------------------------------------------------------------------ |
| `pwd`                 | показать путь               | Проверить, где ты находишься при деплое |
| `ls -la`              | список файлов               | Проверить наличие конфигурации               |
| `cat`,`less`          | читать файл                   | Просмотр логов пайплайна                           |
| `grep`                | поиск по тексту            | Найти ошибки в логах                                    |
| `find`                | поиск файлов                 | Найти конфиги в`/etc`                                       |
| `ps`,`top`            | процессы                        | Проверить, запущен ли nginx                            |
| `df`,`du`             | место на диске              | Проверка свободного места для CI               |
| `systemctl`,`service` | управление сервисами | Перезапустить Docker или Nginx                           |
| `chmod`,`chown`       | права                              | Настроить права на деплой-скрипт             |
| `sudo`                | привилегии                    | Выполнить установку пакета                       |

## Перенаправления и пайплайны (основа автоматизации)

Bash позволяет **перенаправлять потоки ввода/вывода** и объединять команды через **пайплайны** (`|`).

### Применение в DevOps:

* Логи пайплайна → в файл для анализа:
  <pre class="overflow-visible!" data-start="4853" data-end="4898"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre! language-bash"><span><span>./deploy.sh > deploy.log 2>&1
  </span></span></code></div></div></pre>
* Проверка состояния системы:
  <pre class="overflow-visible!" data-start="4931" data-end="4974"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre! language-bash"><span><span>ps aux | grep nginx | </span><span>wc</span><span> -l
  </span></span></code></div></div></pre>
* Автоматический фильтр ошибок:
  <pre class="overflow-visible!" data-start="5009" data-end="5061"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre! language-bash"><span><span>tail</span><span> -f /var/log/syslog | grep error
  </span></span></code></div></div></pre>

## Переменные окружения (ENV) и профили

Переменные окружения хранят параметры системы и приложений.
Файлы конфигурации:

* `~/.bashrc` — для алиасов и персональных настроек;
* `~/.profile`, `/etc/environment` — глобальные переменные.

### Применение в DevOps:


| Пример                              | Назначение                                                          |
| ----------------------------------------- | ----------------------------------------------------------------------------- |
| `export PATH=$PATH:/usr/local/bin`        | Добавить свой путь к утилитам                        |
| `export DOCKER_HOST=tcp://localhost:2375` | Настроить удалённое подключение                  |
| `.env`файлы в Docker                | Хранение переменных окружения приложения |

## Установка и настройка WSL (для Windows DevOps)

WSL позволяет запускать полноценную Linux-среду внутри Windows.
Ты получаешь доступ к bash, apt, systemd, Docker — без виртуалки.

### Применение в DevOps:

* Разработка Linux-скриптов прямо в Windows;
* Тестирование CI/CD сценариев локально;
* Работа с Docker и Kubernetes CLI.

**Пример:**

<pre class="overflow-visible!" data-start="5995" data-end="6086"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre! language-powershell"><span>wsl --install -d Ubuntu
wsl
sudo apt update && sudo apt install docker.io
</span></code></div></div></pre>

## PowerShell в DevOps

### 

PowerShell полезен при DevOps-задачах в Windows-инфраструктуре.
Его преимущество — работа с **объектами**, а не просто текстом.

### Применение:


| Сценарий                      | Пример                                  |
| ------------------------------------- | --------------------------------------------- |
| Управление службами | `Get-Service`,`Restart-Service`               |
| Azure DevOps                          | `Az`модуль для PowerShell            |
| Автоматизация            | `Get-ChildItem`,`Remove-Item`,`Start-Process` |

## Практика

### 🔸 Базовые

1. Найди текущий каталог и выведи все скрытые файлы.
2. Создай структуру `/home/devops/scripts/logs`.
3. Скопируй `.conf` файл из `/etc/nginx/` в свою папку.
4. Найди все `.service` файлы в `/etc/systemd/system`.
5. Проверь, какие процессы используют порт 80.

### 🔸 Средний уровень

6. Найди все `.log` файлы старше 3 дней и удали.
7. Сохрани список пользователей (`/etc/passwd`) в файл `users.txt`.
8. Используя `grep`, найди строки с “failed” в `/var/log/auth.log`.
9. Создай alias `logs="tail -f /var/log/syslog | grep error"`.
10. Настрой переменную `PROJECT_PATH=/home/devops/project` и проверь её.

### 🔸 Продвинутый уровень

11. Напиши однострочник, который подсчитает количество ошибок в логах nginx.
12. Сохрани результат `df -h` в файл с датой в названии.
13. Перенаправь ошибки установки пакета в отдельный файл.
14. Выведи список top-5 процессов по памяти.
15. Перезапусти Docker через `systemctl`.
16. Напиши скрипт, который выводит аптайм и загрузку CPU.
17. Создай резервную копию `/etc` в tar.gz архив.
18. Используя `find`, удали временные файлы `.tmp` старше 2 дней.
19. Проверь, запущен ли nginx (через `pgrep -x nginx`).
20. Напиши alias для быстрой проверки сервисов: `check="systemctl list-units --type=service --state=running"`.
21. Подключись к контейнеру Docker и выведи логи.
22. Сохрани результат `curl -I http://localhost` в файл `check_http.log`.
23. Настрой `sudo` без пароля для своей учётки (тестовая среда).
24. Проверь, какая оболочка по умолчанию.
25. Установи `htop`, запусти и сделай скрин (контроль результата).
