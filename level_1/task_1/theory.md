## 📘 Что такое оболочка (Shell)

**Оболочка (Shell)** — это программа, которая выполняет команды, введённые пользователем, и взаимодействует с ядром операционной системы.
Её можно представить как **переводчика между человеком и системой**.

Схема взаимодействия:

<pre class="overflow-visible!" data-start="1246" data-end="1316"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre!"><span><span>Пользователь → Оболочка (Shell) → Ядро (Kernel) → Оборудование
</span></span></code></div></div></pre>

Оболочка позволяет:

* запускать программы и процессы;
* управлять файлами и каталогами;
* получать доступ к системной информации;
* автоматизировать задачи с помощью **скриптов**.

Без оболочки мы не могли бы управлять системой напрямую.

## Как работает оболочка

1. Пользователь вводит команду, например:

```
ls -l /home
```

1. Shell анализирует ввод и вызывает соответствующую программу (`ls`) с аргументами (`-l /home`).
2. Результат выполнения выводится обратно пользователю.

## Виды оболочек

Существует несколько популярных оболочек. У каждой — свой синтаксис и особенности.


| Название                      | Путь                     | Особенности                                                                                                                    |
| ------------------------------------- | ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| **sh (Bourne Shell)**                 | `/bin/sh`                    | Первая стандартная оболочка UNIX, простая и быстрая.                                              |
| **bash (Bourne Again Shell)**         | `/bin/bash`                  | Самая распространённая. Расширенный синтаксис, переменные, циклы, функции. |
| **zsh (Z Shell)**                     | `/bin/zsh`                   | Улучшенный bash, поддержка автодополнений и тем (Oh My Zsh).                                         |
| **fish (Friendly Interactive Shell)** | `/usr/bin/fish`              | Дружелюбная, цветной вывод, но несовместима с bash-скриптами.                              |
| **PowerShell**                        | `powershell.exe`или`pwsh` | Разработана Microsoft, работает с объектами, а не строками.                                       |

## 📘 Что такое Bash

**Bash (Bourne Again Shell)** — это одна из самых популярных и мощных оболочек.
Она:

* используется по умолчанию в Linux и macOS;
* поддерживается в Docker, WSL, CI/CD;
* сочетает простоту `sh` и дополнительные возможности (функции, массивы, арифметика).

Bash — это **и оболочка, и язык программирования**, предназначенный для автоматизации.

## Почему Bash стал стандартом

Bash стал основным инструментом DevOps-инженера, потому что он:

* установлен почти во всех Linux-дистрибутивах;
* отлично работает в Docker, WSL и CI/CD;
* поддерживает скрипты, переменные, ветвления, циклы;
* универсален — можно запускать как команды, так и целые программы.

## Проверка текущей оболочки

Чтобы узнать, какая оболочка используется сейчас, выполните команду:

```
echo $SHELL
```

или:

```
ps -p $$
```

🔹 `$SHELL` — это переменная окружения, в которой хранится путь к текущей оболочке.
🔹 `ps -p $$` показывает процесс, соответствующий вашей сессии.

## Конфигурационные файлы Bash

Bash при запуске читает свои **конфигурационные файлы** — в них хранятся настройки среды, алиасы и функции.


| Файл           | Назначение                                                               |
| ------------------ | ---------------------------------------------------------------------------------- |
| `~/.bashrc`        | Настройки для интерактивных сессий                  |
| `~/.bash_profile`  | Выполняется при входе в систему                         |
| `/etc/bash.bashrc` | Глобальные настройки для всех пользователей |

## Bash в Windows

На Windows тоже можно использовать Bash.
Для этого есть несколько способов:

1. **WSL (Windows Subsystem for Linux)**
   Позволяет установить полноценную Linux-среду прямо в Windows.
   Пример установки:

   ```
   wsl --install
   ```

   После установки можно запустить Bash через терминал Ubuntu.
2. **Git Bash**
   Идёт вместе с Git for Windows и предоставляет облегчённую среду Bash.

## Примечания

* **Bash используется по умолчанию** на большинстве серверов Linux, поэтому DevOps обязан знать основы.
* **Скрипты Bash** часто применяются в CI/CD, Dockerfile и системных задачах.
* **PowerShell** может работать с объектами, но в DevOps чаще встречается Bash из-за его универсальности.
* Bash имеет свой синтаксис — это полноценный язык, не просто командная строка.
