# 🧭 Блок 2. Навигация по файловой системе: `pwd`, `cd`, `ls`, `tree`

## 🎯 Цель

Научиться свободно перемещаться по структуре файловой системы Linux, понимать логику путей, типы файлов, скрытые элементы и быстро находить нужные данные.
Это основа любого DevOps-процесса — от деплоя приложения до анализа логов или конфигураций.

## 🔹 1. Что такое файловая система Linux

### 📘 Теория

Linux использует **иерархическую файловую систему**, где всё начинается с **корня `/`**.
В отличие от Windows, где есть диски (`C:\`, `D:\`), в Linux — единое дерево каталогов.

**Главные принципы:**

* Всё — это файл (устройства, процессы, сокеты, ссылки).
* Пути бывают **абсолютные** и **относительные**:
  * `/etc/nginx/nginx.conf` — абсолютный путь (от корня).
  * `../scripts/` — относительный (от текущей директории).
* Символ `.` обозначает текущий каталог, `..` — родительский.

### 💼 Применение в DevOps


| Ситуация                                | Пример                 | Что делает DevOps-инженер                                            |
| ----------------------------------------------- | ---------------------------- | ------------------------------------------------------------------------------------ |
| Проверка конфигураций       | `/etc/nginx/`,`/etc/docker/` | Правит файлы конфигураций сервисов                    |
| Анализ логов                         | `/var/log/`                  | Читает ошибки из системных логов                         |
| Деплой проекта                     | `/opt/project/`,`/srv/app/`  | Разворачивает приложение и настраивает права |
| Пользовательские скрипты | `/home/devops/scripts/`      | Хранит собственные утилиты и cron-задачи              |

## 🔹 2. Команда `pwd` — где ты находишься

### 📘 Теория

`pwd` (print working directory) показывает **текущий путь**, в котором ты работаешь.
Это важно при написании скриптов и работе с относительными путями.

<pre class="overflow-visible!" data-start="1799" data-end="1837"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre! language-bash"><span><span>pwd</span><span>
</span><span># /home/devops/scripts</span><span>
</span></span></code></div></div></pre>

### 💼 Применение в DevOps

* Проверить, где находится GitLab Runner при выполнении CI Job.
* Убедиться, что деплой идёт в нужный каталог.
* Перед копированием файлов проверить, из какой директории выполняется скрипт.

**Пример в CI/CD:**

<pre class="overflow-visible!" data-start="2088" data-end="2140"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre! language-bash"><span><span>echo</span><span> </span><span>"Текущая директория пайплайна:"</span><span>
</span><span>pwd</span><span>
</span></span></code></div></div></pre>

## 🔹 3. Команда `cd` — перемещение по каталогам

### 📘 Теория

`cd` (change directory) используется для перемещения в другой каталог.

<pre class="overflow-visible!" data-start="2284" data-end="2327"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre! language-bash"><span><span>cd</span><span> /etc
</span><span>cd</span><span> ..
</span><span>cd</span><span> ~/scripts
</span><span>cd</span><span> -
</span></span></code></div></div></pre>

**Ключевые моменты:**

* `~` — домашний каталог пользователя.
* `cd -` — возвращает в предыдущую директорию.
* `cd /` — переходит в корень системы.

### 💼 Применение в DevOps


| Ситуация                                                     | Пример          |
| -------------------------------------------------------------------- | --------------------- |
| Перейти к логам Nginx                                   | `cd /var/log/nginx`   |
| Перейти в рабочий каталог CI/CD скрипта | `cd /home/devops/app` |
| Вернуться в предыдущую директорию      | `cd -`                |
| Перейти в каталог скриптов                    | `cd ~/scripts`        |

> В пайплайнах CI часто требуется переход в нужный путь перед сборкой или деплоем.

## 🔹 4. Команда `ls` — просмотр содержимого каталогов

### 📘 Теория

`ls` отображает список файлов и папок.
Флаги меняют формат вывода:


| Флаг | Значение                                                     | Пример |
| -------- | -------------------------------------------------------------------- | ------------ |
| `-l`     | подробный список (размер, дата, права) | `ls -l`      |
| `-a`     | показать скрытые файлы (`.env`,`.git`)           | `ls -a`      |
| `-h`     | читаемый формат размера (KB/MB)                 | `ls -lh`     |
| `-t`     | сортировать по времени                           | `ls -lt`     |
| `-R`     | рекурсивно по подкаталогам                   | `ls -R`      |

**Пример:**

<pre class="overflow-visible!" data-start="3339" data-end="3369"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre! language-bash"><span><span>ls</span><span> -lah /etc/nginx
</span></span></code></div></div></pre>

### 💼 Применение в DevOps

* Проверить конфигурации сервиса:
  `ls -l /etc/systemd/system`
* Проверить права на деплой-скрипты:
  `ls -l ~/deploy.sh`
* Проверить, есть ли скрытые `.env` или `.gitignore` файлы.
* Сортировать логи по времени изменения:
  `ls -lt /var/log/`

## 🔹 5. Команда `tree` — визуальное отображение структуры

### 📘 Теория

`tree` выводит древовидную структуру каталогов.
Устанавливается вручную:

<pre class="overflow-visible!" data-start="3814" data-end="3847"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre! language-bash"><span><span>sudo apt install tree
</span></span></code></div></div></pre>

**Пример:**

<pre class="overflow-visible!" data-start="3862" data-end="3899"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre! language-bash"><span><span>tree /home/devops/scripts
</span></span></code></div></div></pre>

**Флаги:**

* `-L 2` — глубина отображения (например, только 2 уровня).
* `-d` — показывать только папки.

### 💼 Применение в DevOps


| Сценарий                                                             | Пример                |
| ---------------------------------------------------------------------------- | --------------------------- |
| Просмотр структуры проекта перед деплоем | `tree -L 2 /opt/app`        |
| Проверка структуры конфигов                         | `tree /etc/nginx/`          |
| Документация инфраструктуры                        | `tree /srv > structure.txt` |

## 🔹 6. Абсолютные и относительные пути

### 📘 Теория

* **Абсолютный путь:** начинается с `/`
  Пример: `/home/devops/scripts/deploy.sh`
* **Относительный путь:** от текущего каталога
  Пример: `../logs/error.log`

**Комбинированный пример:**

<pre class="overflow-visible!" data-start="4526" data-end="4566"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre! language-bash"><span><span>cd</span><span> /var/log
</span><span>cat</span><span> ../etc/hosts
</span></span></code></div></div></pre>

### 💼 Применение в DevOps

* При написании CI/CD скриптов важно понимать, откуда запускается скрипт.
* Docker COPY использует относительные пути.
* В Ansible playbook'ах и Terraform важны точные пути к файлам конфигурации.

## 🔹 7. Навигация между системами (SSH + файловая структура)

### 📘 Теория

DevOps-инженер часто работает с **удалёнными системами** через `ssh`.
После подключения нужно уметь быстро ориентироваться по структуре.

<pre class="overflow-visible!" data-start="5025" data-end="5083"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre! language-bash"><span><span>ssh devops@192.168.1.10
</span><span>pwd</span><span>
</span><span>ls</span><span> -la /etc/nginx/
</span></span></code></div></div></pre>

### 💼 Применение

* Быстрая диагностика серверов: просмотр `/var/log/`, `/etc/`, `/home/`.
* Проверка конфигураций или логов на продакшн-серверах.
* Навигация перед деплоем или копированием файлов.

## 🔹 8. Практика (25 задач)

### 🔸 Базовые задачи

1. Определи, где ты находишься (`pwd`).
2. Перейди в каталог `/etc` и выведи его содержимое.
3. Вернись в домашний каталог с помощью `~`.
4. Выведи скрытые файлы (`ls -a`).
5. Перейди в `/var/log` и посмотри список файлов с датами (`ls -lh`).
6. Покажи только каталоги в `/home`.
7. Используя `cd -`, вернись в предыдущий путь.
8. Сравни вывод `ls` и `ls -l`.
9. Найди конфигурацию nginx в `/etc/nginx/`.
10. Используя `tree`, выведи структуру `/etc/systemd`.

### 🔸 Средний уровень

11. Покажи только файлы `.service` в `/etc/systemd/system`.
12. С помощью `tree -L 2`, выведи структуру `/var/log`.
13. Создай каталог `/home/devops/test1/scripts/logs` одной командой.
14. Перейди в новый каталог и выведи полный путь (`pwd`).
15. Проверь, сколько уровней каталогов до корня.
16. Выведи список файлов в `/usr/bin`, отсортированных по времени.
17. Сравни абсолютный и относительный путь до файла `/etc/hosts`.
18. Используя `ls`, проверь права на `/etc/ssh/sshd_config`.
19. Найди, где лежит бинарник `bash` (`which bash`).
20. Сохрани вывод `tree /etc/nginx` в файл `nginx_structure.txt`.

### 🔸 Продвинутый уровень

21. Подключись по SSH к удалённому серверу и выведи список логов (`ls -lh /var/log`).
22. Используя `tree`, создай карту каталогов проекта `/opt/app`.
23. Создай alias `goetc="cd /etc"`.
24. Используя относительный путь, перейди на уровень выше из `/home/devops/scripts/`.
25. Напиши скрипт `navigate.sh`, который:
    * Показывает текущий каталог;
    * Переходит в `/var/log`;
    * Показывает 5 последних изменённых файлов;
    * Возвращается обратно и выводит сообщение `"Навигация завершена."`
