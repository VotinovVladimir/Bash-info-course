## 📘 Навигация по файловой системе

Навигация по файловой системе — это фундаментальный навык работы в терминале. Любой DevOps-инженер или системный администратор ежедневно взаимодействует с каталогами и файлами через командную строку. Понимание структуры директорий, умение перемещаться между ними и просматривать содержимое является базой для работы с серверами, Docker-контейнерами и автоматизацией задач с помощью скриптов.

Файловая система представляет собой иерархическую структуру папок и файлов. В Linux, macOS и WSL корнем этой структуры является каталог `/`. Все остальные папки располагаются относительно него. На Windows аналогичная концепция работает через диски (`C:\`, `D:\` и т.д.), но в WSL используется полноценная Linux-структура.

### Команда `pwd`

Команда `pwd` используется для определения текущей рабочей директории. Аббревиатура `pwd` расшифровывается как **print working directory** — «вывести рабочую директорию». Это важно, чтобы всегда знать, где вы находитесь в файловой системе, особенно при работе с командами, которые создают, перемещают или удаляют файлы.

Пример использования:

<pre class="overflow-visible!" data-start="1281" data-end="1296"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre! language-bash"><span><span>pwd</span><span>
</span></span></code></div></div></pre>

После выполнения этой команды терминал выведет полный путь текущей директории, начиная с корня. Например:

<pre class="overflow-visible!" data-start="1407" data-end="1435"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre!"><span><span>/home/user/Documents
</span></span></code></div></div></pre>

Полезно помнить, что путь, который выводит `pwd`, всегда абсолютный, и чувствителен к регистру символов в Linux.

### Команда `cd`

Команда `cd` предназначена для перемещения между директориями. Аббревиатура `cd` означает **change directory** — «сменить директорию».

Вы можете перемещаться как по абсолютным путям, так и по относительным. Абсолютный путь начинается от корня `/`, а относительный — от текущей директории.

Примеры использования:

<pre class="overflow-visible!" data-start="1897" data-end="2154"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre! language-bash"><span><span>cd</span><span> /home/user/Documents    </span><span># перейти в конкретную папку</span><span>
</span><span>cd</span><span> ..                      </span><span># подняться на уровень выше</span><span>
</span><span>cd</span><span> ~                       </span><span># перейти в домашнюю директорию пользователя</span><span>
</span><span>cd</span><span> -                       </span><span># вернуться в предыдущую директорию</span><span>
</span></span></code></div></div></pre>

Особенно полезно знать, что символ `~` обозначает домашнюю директорию текущего пользователя, а `..` — родительскую директорию.

### Команда `ls`

Команда `ls` позволяет просматривать содержимое каталогов. Аббревиатура `ls` расшифровывается как **list** — «список». Эта команда является одной из самых часто используемых в терминале.

Примеры использования:

<pre class="overflow-visible!" data-start="2525" data-end="2861"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre! language-bash"><span><span>ls</span><span>          </span><span># выводит имена файлов и папок в текущей директории</span><span>
</span><span>ls</span><span> -l       </span><span># подробный список с правами доступа, владельцем, размером и датой изменения</span><span>
</span><span>ls</span><span> -a       </span><span># показывает скрытые файлы и папки (начинаются с .)</span><span>
</span><span>ls</span><span> -lh      </span><span># человеко-читаемый формат размеров файлов</span><span>
</span><span>ls</span><span> -R       </span><span># рекурсивный просмотр всех подкаталогов</span><span>
</span></span></code></div></div></pre>

Комбинируя ключи, можно получать различные виды информации. Например, `ls -la` показывает все файлы с подробностями, включая скрытые.

### Команда `tree`

Команда `tree` выводит структуру каталогов в виде дерева. Она полезна для визуального анализа иерархии папок и понимания расположения файлов. Аббревиатура `tree` буквально переводится как «дерево».

Примеры использования:

<pre class="overflow-visible!" data-start="3254" data-end="3426"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre! language-bash"><span><span>tree         </span><span># выводит дерево текущей директории</span><span>
tree -L 2    </span><span># ограничивает глубину дерева до двух уровней</span><span>
tree -a      </span><span># показывает все файлы, включая скрытые</span><span>
</span></span></code></div></div></pre>

В некоторых Linux-системах `tree` может быть не установлена по умолчанию. Установить её можно командой:

<pre class="overflow-visible!" data-start="3535" data-end="3591"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre! language-bash"><span><span>sudo apt install tree    </span><span># для Ubuntu/Debian</span><span>
</span></span></code></div></div></pre>

### Полезные заметки

Навигация по файловой системе — основа для всех последующих команд, таких как создание, копирование и удаление файлов. Автодополнение с помощью клавиши `Tab` значительно ускоряет ввод путей и имен файлов.

Очень важно понимать разницу между абсолютными и относительными путями, чтобы избегать ошибок при работе с директориями. Команды `ls` и `tree` дают разные представления: первая — это список файлов и папок, вторая — визуальная иерархия.

Все эти навыки пригодятся при работе в Linux, WSL, Docker-контейнерах и при автоматизации задач с помощью Bash-скриптов.
