# 0-1.**Полный курс Bash с углубленной теорией и практикой**

### **Уровень 0–1: Основы команд и терминала (25 задач)**

**Цель:** освоить Bash, терминал, работу с файлами и переменные.

**Теория и пункты:**

1. [Введение в Bash: что такое оболочка, зачем нужна, типы оболочек](./level_1/1.md)
2. Терминал Linux и Windows (WSL, PowerShell)
3. Навигация по файловой системе: `pwd`, `cd`, `ls`, `tree`
4. Работа с файлами: `touch`, `rm`, `cp`, `mv`
5. Работа с каталогами: `mkdir`, `rmdir`, `rm -r`
6. Просмотр содержимого файлов: `cat`, `less`, `more`, `head`, `tail`
7. Перенаправление вывода: `>`, `>>`, `<`, `2>`, `&>`
8. Пайпы: `|`
9. Переменные Bash: создание, использование, экспорт
10. Специальные переменные: `$?`, `$#`, `$0`, `$1` … `$9`, `$@`, `$*`
11. Кавычки: `"`, `'`, ```
12. Простые арифметические операции: `expr`, `$(( ))`
13. Управление пользователями: `whoami`, `id`, `groups`
14. Права доступа к файлам: `chmod`, `chown`, `chgrp`
15. Проверка прав: `ls -l`, `stat`
16. История команд и автодополнение: `history`, `!!`, `!n`, `TAB`
17. Aliases и функции для упрощения команд
18. Комментарии в скриптах
19. Основы отладки скриптов: `set -x`, `set -e`
20. Создание первого простого скрипта и его запуск
21. Различия между интерактивным терминалом и скриптом
22. Основы логирования: вывод в файл
23. Условные конструкции для простых задач
24. Чтение ввода пользователя: `read`
25. Проверка существования файлов и каталогов

**Практические задачи:**

1. Скрипт «Hello, World»
2. Скрипт для вывода текущего каталога и пользователя
3. Скрипт для создания каталога с вложенными подкаталогами
4. Скрипт для создания и удаления файлов
5. Скрипт для копирования файлов
6. Скрипт для переименования файлов
7. Скрипт для просмотра первых и последних строк файла
8. Скрипт для подсчета строк в файле
9. Скрипт для объединения нескольких файлов
10. Скрипт для проверки наличия файла
11. Скрипт для проверки наличия каталога и его создания
12. Скрипт для автоматического резервного копирования каталога
13. Скрипт для вывода переменных окружения
14. Скрипт для вывода специальных переменных (`$?`, `$#`, `$@`)
15. Скрипт для арифметической операции (сложение/вычитание двух чисел)
16. Скрипт для изменения прав доступа к файлу
17. Скрипт для проверки прав на файл или каталог
18. Скрипт для чтения имени пользователя и вывода приветствия
19. Скрипт с логированием команд в файл
20. Скрипт для фильтрации вывода команды `ls`
21. Скрипт для использования alias и функции
22. Скрипт с проверкой ввода числа от пользователя
23. Скрипт для автоматического создания структуры каталогов и файлов
24. Скрипт для проверки пустоты каталога и уведомления об этом
25. Скрипт, объединяющий ввод пользователя, проверку условий и логирование

### **Уровень 2: Условия, логика и ветвления (25 задач)**

**Теория и пункты:**

1. Операторы сравнения чисел: `-eq`, `-ne`, `-lt`, `-le`, `-gt`, `-ge`
2. Операторы сравнения строк: `==`, `!=`, `<`, `>`
3. Логические операторы: `&&`, `||`, `!`
4. Ветвления `if`, `elif`, `else`
5. Вложенные условия
6. Проверка существования файла: `-f`
7. Проверка существования каталога: `-d`
8. Проверка на пустоту файла: `-s`
9. Проверка прав доступа: `-r`, `-w`, `-x`
10. Использование `case`
11. Команды с возвратным кодом `$?`
12. Проверка количества аргументов `$#`
13. Использование `test` и `[ ]`
14. Проверка числовых диапазонов
15. Проверка переменной на пустоту
16. Сравнение строк с использованием регулярных выражений
17. Условные сокращения (`&& … || …`)
18. Чтение пользовательского ввода для условий
19. Проверка команд через `command -v`
20. Ветвления с `[[ … ]]`
21. Использование `let` и арифметических выражений внутри условий
22. Управление ошибками и уведомления об ошибках
23. Использование `if` для работы с процессами
24. Использование условий для работы с сетевыми командами
25. Подготовка скрипта с комбинацией всех изученных условий

## **Уровень 3: Циклы и функции (25 задач)**

**Цель:** освоить циклы и функции, научиться автоматизировать повторяющиеся действия.

**Теория и пункты:**

1. Цикл `for` для чисел
2. Цикл `for` для файлов и строк
3. Цикл `while`
4. Цикл `until`
5. Вложенные циклы
6. Использование `break` и `continue`
7. Перебор массивов в циклах
8. Использование циклов для логирования
9. Циклы с условием (`while [ условие ]`)
10. Использование циклов для обработки пользовательского ввода
11. Циклы для работы с процессами
12. Циклы для работы с сетевыми командами
13. Введение в функции
14. Определение функций (`function_name() { … }`)
15. Вызов функций
16. Передача аргументов в функции (`$1`, `$2`)
17. Возврат значений через `return`
18. Возврат значений через `echo`
19. Локальные переменные внутри функций (`local`)
20. Глобальные переменные и области видимости
21. Использование функций для повторного кода
22. Использование функций для обработки ошибок
23. Вложенные функции
24. Отладка функций и циклов (`set -x`, `set -e`)
25. Подготовка комплексного скрипта с циклами и функциями

**Практические задачи (25 задач):**

* Простые циклы по числам и строкам
* Скрипт для подсчета файлов в каталоге
* Скрипт для обработки логов построчно
* Скрипт для суммирования чисел из файла
* Скрипт для поиска файлов по маске
* Скрипт с функцией логирования действий
* Скрипт с функцией проверки файла или каталога
* Скрипт для резервного копирования с функцией
* Скрипт с циклом, который опрашивает сетевой сервис
* Скрипт для перебора всех процессов и вывода статуса
* Скрипт для переименования всех файлов в каталоге
* Скрипт для удаления файлов старше N дней
* Скрипт для подсчета строк в нескольких файлах
* Скрипт для обработки CSV-файлов
* Скрипт с пользовательским вводом внутри цикла
* Скрипт для автоматизации повторяющейся задачи
* Скрипт для уведомления по результату проверки файлов
* Скрипт с вложенными циклами для обработки каталогов
* Скрипт для фильтрации и подсчета логов
* Скрипт с функцией проверки наличия команды (`command -v`)
* Скрипт с возвратом ошибок через `return`
* Скрипт с логированием ошибок в отдельный файл
* Скрипт для тестирования веб-сайтов через цикл `curl`
* Скрипт для автоматической архивации файлов
* Итоговый комплексный скрипт: резервное копирование + логирование + уведомление

## **Уровень 4: Массивы, строки и регулярные выражения (25 задач)**

**Цель:** научиться работать с массивами, строками и шаблонами.

**Теория и пункты:**

1. Индексированные массивы: создание, доступ к элементам
2. Ассоциативные массивы
3. Добавление и удаление элементов массива
4. Длина массива и перебор элементов
5. Итерация по массиву в цикле
6. Массивы с аргументами скрипта (`$@`)
7. Строковые операции: конкатенация, подстрока
8. Изменение регистра: `${VAR,,}`, `${VAR^^}`
9. Удаление символов/шаблонов: `${VAR#}`, `${VAR##}`, `${VAR%}`, `${VAR%%}`
10. Подстановка значений по умолчанию: `${VAR:-default}`
11. Проверка пустой строки
12. Разделение строки на массив
13. Сравнение строк
14. Использование регулярных выражений в `[[ … =~ … ]]`
15. Использование `grep` для поиска шаблонов
16. Использование `sed` для замены текста
17. Использование `awk` для разбора строк
18. Подстановка результатов команд в переменные: `VAR=$(command)`
19. Форматирование вывода
20. Объединение массивов и строк
21. Фильтрация данных по шаблону
22. Проверка наличия элемента в массиве
23. Сортировка и уникальные элементы массива
24. Удаление пробелов и лишних символов
25. Подготовка комплексного скрипта для обработки строк и массивов

**Практические задачи (25 задач):**

* Скрипт для создания массива файлов в каталоге
* Скрипт для перебора массива и вывода всех элементов
* Скрипт для добавления и удаления элементов массива
* Скрипт для подсчета количества элементов
* Скрипт для преобразования регистра текста
* Скрипт для извлечения подстроки
* Скрипт для замены текста в файле с `sed`
* Скрипт для фильтрации логов с `grep`
* Скрипт для разбора CSV-файла с `awk`
* Скрипт для объединения массивов
* Скрипт для проверки наличия элемента в массиве
* Скрипт для сортировки массива
* Скрипт для удаления дубликатов
* Скрипт для разделения строки на массив
* Скрипт для форматирования вывода логов
* Скрипт для анализа списка пользователей
* Скрипт для фильтрации процессов по имени
* Скрипт для проверки шаблонов в файлах
* Скрипт для извлечения IP-адресов из логов
* Скрипт с циклом по массиву и условиями
* Скрипт для замены текста в нескольких файлах
* Скрипт для логирования ошибок и фильтрации по ключевым словам
* Скрипт для подсчета уникальных значений
* Скрипт для подготовки отчета по логам
* Итоговый комплексный скрипт: сбор логов, фильтрация, форматирование, уведомление

## **Уровень 5: Работа с процессами, сетью и планировщиком задач (25 задач)**

**Цель:** научиться управлять процессами, проверять состояние сервисов, автоматизировать задачи с cron и `systemd timers`.

**Теория и пункты:**

1. Просмотр процессов: `ps`, `top`, `htop`, `pgrep`
2. Управление процессами: `kill`, `pkill`, `killall`
3. Нахождение процессов по имени или PID
4. Фоновый запуск команд: `&`, `nohup`
5. Проверка состояния команд и процессов (`$?`)
6. Приоритет процессов: `nice`, `renice`
7. Работа с логами процессов: `journalctl`, `tail -f`
8. Проверка сетевых соединений: `ping`, `curl`, `wget`
9. Проверка портов и соединений: `netstat`, `ss`
10. Диагностика сети: `traceroute`, `dig`, `nslookup`
11. Использование условий для проверки состояния процессов
12. Автоматическая проверка доступности веб-сервисов
13. Настройка планировщика задач `cron`
14. Синтаксис crontab: минуты, часы, дни, месяцы, дни недели
15. Работа с `systemd timers` как альтернативой cron
16. Настройка повторяющихся задач с интервалом
17. Логирование выполнения задач
18. Отправка уведомлений по завершению задач
19. Проверка выполнения команды и повтор при ошибке
20. Комбинирование цикла и планировщика задач
21. Использование скриптов для автоматизации обслуживания
22. Автоматическая очистка временных файлов
23. Мониторинг дискового пространства и уведомления
24. Отправка отчётов о состоянии сервиса
25. Подготовка комплексного скрипта: мониторинг сервиса + уведомление + логирование

**Практические задачи (25 задач):**

1. Скрипт для проверки запущенного процесса по имени
2. Скрипт для завершения процесса по PID
3. Скрипт для запуска процесса в фоне с `nohup`
4. Скрипт для проверки состояния веб-сайта через `curl`
5. Скрипт для мониторинга CPU и памяти процесса
6. Скрипт для уведомления при превышении лимита памяти процесса
7. Скрипт для проверки доступности нескольких серверов
8. Скрипт для проверки открытых портов на локальной машине
9. Скрипт для логирования ошибок процесса
10. Скрипт для мониторинга логов с `tail -f`
11. Скрипт для отправки уведомления при падении сервиса
12. Скрипт для автоматической очистки временных файлов
13. Скрипт для архивации логов через cron
14. Скрипт для резервного копирования каталога через cron
15. Скрипт для планового запуска скрипта каждый день
16. Скрипт для проверки наличия процесса и его перезапуска
17. Скрипт для подсчета количества запущенных процессов
18. Скрипт для мониторинга сети и записи результатов
19. Скрипт для проверки HTTP-кодов нескольких сайтов
20. Скрипт с циклом по списку серверов и проверкой доступности
21. Скрипт для отправки уведомления на email/Telegram
22. Скрипт для автоматической очистки старых бэкапов
23. Скрипт для проверки свободного места на диске
24. Скрипт для автоматического удаления файлов старше N дней
25. Итоговый комплексный скрипт: мониторинг, уведомление, логирование, очистка

## **Уровень 6: Итоговые проекты и комплексные скрипты (25 задач/проектов)**

**Цель:** объединить все изученные темы в практических проектах, как на реальной работе DevOps или на собеседованиях.

**Проекты и задачи:**

1. **Проект 1: Мониторинг веб-сервиса**
   * Проверка доступности сайта через `curl`
   * Логирование HTTP-кодов
   * Уведомление при падении
   * Автоматический перезапуск сервиса
   * Расширение: поддержка нескольких сайтов
2. **Проект 2: Резервное копирование и архивация**
   * Создание бэкапа каталога
   * Сжатие в `tar.gz`
   * Добавление даты в имя архива
   * Удаление старых архивов
   * Настройка cron для автоматизации
3. **Проект 3: Анализ и фильтрация логов**
   * Разбор лог-файлов с `grep`, `awk`
   * Вывод статистики ошибок
   * Формирование отчёта
   * Сохранение отчёта в файл
   * Автоматизация анализа через cron
4. **Проект 4: Автоматизация повседневных задач**
   * Проверка свободного места на диске
   * Очистка временных файлов
   * Проверка запущенных процессов
   * Логирование результатов
   * Отправка уведомлений по завершению
5. **Проект 5: Интерактивные скрипты с вводом пользователя**
   * Меню выбора действий
   * Поддержка нескольких опций (резервное копирование, мониторинг, очистка)
   * Проверка введенных данных
   * Логирование действий
   * Расширение для работы с несколькими директориями
6. **Проект 6: Сборка комплексного DevOps-скрипта**
   * Проверка состояния процессов
   * Мониторинг веб-сервиса
   * Резервное копирование
   * Очистка старых логов и бэкапов
   * Формирование отчёта и уведомление
